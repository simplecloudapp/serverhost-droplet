paths:
  - path: "server.properties"
    type: "PROPERTIES"
    data:
      server-ip: "%ip%"
      server-port: %port%
      max-players: %max-players%
      online-mode: false
  - path: "config/paper-global.yml"
    type: "YML"
    data:
      proxies:
        velocity:
          enabled: "true"
          online-mode: "true"
          secret: "%forwarding-secret%"